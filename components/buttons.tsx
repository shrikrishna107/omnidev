import React from 'react'

const buttons = ({ ext, onclic}) => {
  return (
    <button
    type="button"
    style={{ margin: 10 }}
    onClick={onClick}
  >
    {text}
  </button>
  )
}

export default buttons